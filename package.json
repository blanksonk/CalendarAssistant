{
  "name": "calendar-assistant",
  "private": true,
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,green \"npm run dev --prefix client\" \"python -m uvicorn server.main:app --reload --port 8000\"",
    "build": "npm run build --prefix client",
    "test": "npm run test --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
